is와 as를 통해 클래스간의 형변환이 가능한지 확인할 수 있다.
부모와 자식클래스 양방향으로 확인 할 수있다.
is는 형변환 여부를 Bool형식으로 반환하고
as는 형변환이 가능하면 형변환을 실행하여 반환하고 아니면 null값을 반환한다.

class AAA{
}

class BBB:AAA{
}

if(AAA is BBB)
  print("형변환 가능");
  
if(BBB is AAA)
  print("형변환 가능");
